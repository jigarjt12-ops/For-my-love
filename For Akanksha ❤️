<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Happy Rose Day 🌹</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@500;700&display=swap" rel="stylesheet">

  <style>
    body {
      margin: 0;
      height: 100vh;
      background: #0b0b14;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      font-family: 'Baloo 2', cursive;
      color: white;
      overflow: hidden;
      text-align: center;
    }

    h1 {
      font-size: 36px;
      margin-bottom: 10px;
      z-index: 2;
    }

    h2 {
      font-size: 19px;
      opacity: 0.9;
      margin-bottom: 30px;
      max-width: 90%;
      z-index: 2;
    }

    .buttons {
      position: relative;
      width: 100%;
      max-width: 600px;
      height: 200px;
      z-index: 2;
    }

    button {
      padding: 14px 32px;
      border-radius: 40px;
      border: none;
      font-size: 18px;
      cursor: pointer;
      position: absolute;
      transition: 0.25s ease;
      font-family: 'Baloo 2', cursive;
      white-space: nowrap;
      z-index: 2;
    }

    #yes {
      background: linear-gradient(135deg, #ff4d88, #ff85a2);
      color: white;
      left: 50%;
      transform: translateX(-50%);
      bottom: 10px;
      box-shadow: 0 0 25px rgba(255, 77, 136, 0.6);
    }

    #yes:hover {
      transform: translateX(-50%) scale(1.1);
    }

    #no {
      background: #1f1f33;
      color: white;
      left: 20%;
      top: 20px;
    }

    .heart {
      position: fixed;
      animation: float linear infinite;
      opacity: 0.7;
      pointer-events: none;
      z-index: 1;
    }

    @keyframes float {
      from {
        transform: translateY(20px) scale(1);
      }
      to {
        transform: translateY(-120px) scale(1.25);
      }
    }
  </style>
</head>

<body><audio id="myMusic" loop>
    <source src="https://od.lk/s/MTJfMjMyMzA3MTFf/song.MP3" type="audio/mpeg">
</audio>
<div class="lyrics-container">
</div>
<script>
    // Unlocks and plays audio on the very first touch/click
    document.addEventListener('click', function() {
        const audio = document.getElementById('myMusic');
        audio.play().catch(e => console.log("Audio waiting for user interaction."));
    }, { once: true });


  <h1>Hey beautiful 🌹✨</h1>
  <h2>
    Roses are red 🌹, skies are blue 💫  
    But none of them are as lovely as you 🥰❤️
  </h2>

  <h2>
    So today, on Rose Day 🌸  
    Will you accept this rose from me? 💐😊
  </h2>

  <div class="buttons">
    <button id="yes">YES 🌹😍</button>
    <button id="no">NO 🙈</button>
  </div>

  <script>
    const noBtn = document.getElementById("no");
    const yesBtn = document.getElementById("yes");
    const container = document.querySelector(".buttons");

    const noTexts = [
      "NO 🙈","Are you sure? 🤨","Really?? 😳","Think again 😏",
      "It’s just a rose 🥺","Don’t break my heart 💔",
      "Pleaseee 🌹","One little rose 🥹",
      "Still no? 😢","Okay okay… YES 🌹😍"
    ];

    let noIndex = 0;
    let locked = false;

    noBtn.addEventListener("mouseenter", () => {
      if (locked) return;

      noBtn.innerText = noTexts[noIndex];

      if (noIndex === noTexts.length - 1) {
        locked = true;
        noBtn.style.background =
          "linear-gradient(135deg, #ff4d88, #ff85a2)";
        noBtn.onclick = () => yesBtn.click();
        return;
      }

      noIndex++;

      setTimeout(() => {
        const maxX = container.clientWidth - noBtn.offsetWidth - 10;
        const minY = 10;
        const maxY =
          container.clientHeight / 2 - noBtn.offsetHeight / 2;

        noBtn.style.left = Math.random() * maxX + "px";
        noBtn.style.top =
          minY + Math.random() * (maxY - minY) + "px";
      }, 120);
    });

    yesBtn.addEventListener("click", () => {
      document.body.innerHTML = `
        <div style="
          height:100vh;
          display:flex;
          flex-direction:column;
          justify-content:center;
          align-items:center;
          background:#0b0b14;
          color:white;
          text-align:center;
          font-family:'Baloo 2', cursive;">
          <h1 style="font-size:38px;">Happy Rose Day 🌹💖</h1>
          <p style="font-size:22px;">
            This rose is a symbol of my feelings for you 😌❤️
          </p>
          <p>
            May our days be as beautiful as roses 🌸✨
          </p>
        </div>
      `;
    });

    /* 🌹 ROSE DAY BACKGROUND EMOJIS 🌹 */
    setInterval(() => {
      for (let i = 0; i < 2; i++) {
        const e = document.createElement("div");
        e.className = "heart";
        e.innerHTML = [
          "🌹","🌸","💐","❤️","💖","💕",
          "✨","🥰","😍","🎀"
        ][Math.floor(Math.random() * 10)];

        e.style.left = Math.random() * 100 + "vw";
        e.style.top = Math.random() * 100 + "vh";
        e.style.fontSize = 6 + Math.random() * 9 + "px";
        e.style.animationDuration = 5 + Math.random() * 4 + "s";

        document.body.appendChild(e);
        setTimeout(() => e.remove(), 10000);
      }
    }, 190);
  </script>
</script>
</body>
</html>
